import React from 'react';

const AddBox = () => (
  <form>
    <input type="text" placeholder="Insira o nome de usuário aqui..." />
    <button type="submit">OK!</button>
  </form>
);

export default AddBox;
